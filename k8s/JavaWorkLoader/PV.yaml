apiVersion: v1
kind: PersistentVolume
metadata:
  name: jar-storage
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  hostPath:
    path: /data/jar-runner